---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: cappbuilds.rcs.dana.io
spec:
  group: rcs.dana.io
  names:
    kind: CappBuild
    listKind: CappBuildList
    plural: cappbuilds
    singular: cappbuild
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CappBuild is the Schema for the cappbuilds API. It represents a build-from-
          source request and its observed state.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the CappBuild.
            properties:
              buildFile:
                description: BuildFile indicates whether the source should be built
                  using a buildfile-based or non-buildfile-based strategy.
                properties:
                  mode:
                    description: |-
                      Mode selects whether the source is expected to contain a Dockerfile/Containerfile.
                      Present: use a buildfile-based strategy; Absent: use a non-buildfile-based strategy.
                    enum:
                    - Present
                    - Absent
                    type: string
                required:
                - mode
                type: object
              cappRef:
                description: |-
                  CappRef links this build to a Capp. If set, the controller will update the
                  Capp with the built image.
                properties:
                  containerName:
                    description: |-
                      ContainerName selects which container in the referenced Capp should have its image updated.
                      If omitted, the first container in the Capp pod template is updated.
                    type: string
                  name:
                    description: Name is the name of the Capp resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              output:
                description: Output refers to the location where the built image would
                  be pushed.
                properties:
                  image:
                    description: Image is the reference of the image.
                    minLength: 1
                    type: string
                  pushSecret:
                    description: Describes the secret name for pushing a container
                      image.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - image
                type: object
              rebuild:
                description: Rebuild controls rebuild behavior.
                properties:
                  mode:
                    description: Mode selects the rebuild strategy.
                    enum:
                    - Initial
                    - OnCommit
                    type: string
                required:
                - mode
                type: object
              source:
                description: Source refers to the location where the source code is.
                properties:
                  contextDir:
                    description: |-
                      ContextDir is a path to a subdirectory within the source code that should
                      be used as the build root directory.
                    type: string
                  git:
                    description: Git contains the details for obtaining source code
                      from a git repository.
                    properties:
                      cloneSecret:
                        description: |-
                          CloneSecret references a Secret that contains credentials to access the
                          repository.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      revision:
                        description: |-
                          Revision describes the Git revision (e.g., branch, tag, commit SHA, etc.)
                          to fetch.
                        type: string
                      url:
                        description: URL describes the URL of the Git repository.
                        minLength: 1
                        type: string
                    required:
                    - url
                    type: object
                  type:
                    description: |-
                      Type is the type of source code used as input for the build.
                      Supported values: "Git".
                    enum:
                    - Git
                    type: string
                required:
                - git
                - type
                type: object
            required:
            - buildFile
            - output
            - source
            type: object
          status:
            description: Status defines the observed state of the CappBuild.
            properties:
              buildRef:
                description: BuildRef references the associated Build.
                type: string
              conditions:
                description: |-
                  Conditions represent the latest available observations of the CappBuild's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBuildRunRef:
                description: |-
                  LastBuildRunRef is a reference to the last BuildRun
                  created for this CappBuild.
                type: string
              latestImage:
                description: LatestImage is the last successfully produced image reference.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the .metadata.generation last processed by the
                  controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
